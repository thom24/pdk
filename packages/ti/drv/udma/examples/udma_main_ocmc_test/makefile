#
# This file is the makefile for building UDMA main ocmc test app
#

ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = udma_main_ocmc_testapp_$(BUILD_OS_TYPE)

include makefile.mk
